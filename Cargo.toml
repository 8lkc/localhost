[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"
mio = { version = "1.0.3", features = ["net", "os-poll"] }
serde = { version = "1.0.216", features = ["derive"] }
toml = "0.8.19"
winnow = "0.7.2"
