[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
http = "1.2.0"
libc = "0.2.169"
mio = {version = "1.0.3", features = ["net", "os-poll"]}
serde = { version = "1.0.217", features = ["derive"] }
